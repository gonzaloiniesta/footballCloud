version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    networks:
      - football_network

  # fastapi:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: fastapi
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - ./app:/app
  #   depends_on:
  #     - mongodb
  #   networks:
  #     - football_network

volumes:
  mongodb_data:

networks:
  football_network:
    driver: bridge
